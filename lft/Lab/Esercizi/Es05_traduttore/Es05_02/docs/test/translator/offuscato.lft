// Autore: Andrea Delmastro
// Il programma esegue cicli e istruzioni annidate applicando somme e sottrazioni
// ad un numero preso in input. Il risultato deve sempre essere uguale al numero
// inserito.

= i 0;
read(c);
&& true false;
while(< i 10) {
    cond
        when(< i 5) do {
            = j +(0 68 i - 0 i - 0 68);
            while(< j 2) {
                = c +(c 1);
                = a 0;
                while(< a 2) {
                    cond
                        when(== a a) do = a *(a 1)
                        else = j j;
                    cond
                        when(== a 0) do = j +(j a - 0 a 0)
                        else = j +(j *(1 /a a));
                    = a +(a 1)
                 }
            }
        }
        else {
            = j *(+(---*(i 3) i +(i - 0 15) i - 0 15));
            while(< j 2) {
                = c - c 1;
                = j +(j 1 --*(j 4) j j j j)
            }
        };
    = i +(i +(*(4 4) 4 - 0 19))
};
print(c)